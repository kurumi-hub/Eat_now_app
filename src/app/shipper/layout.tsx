import type { ReactNode } from "react";
import "@/styles/order-process.css";
import "@/styles/shipper.css";

export default function ShipperLayout({ children }: { children: ReactNode }) { return children; }
